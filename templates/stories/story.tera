{% import "components/tasks/create" as tasks %}
{% include "head" %}

<body>
{% include "header" %}

<style>
  .listitem {
    display: none;
    transition: all 0.3s ease;
  }

  .listitem.visible {
    display: block;
  }
</style>

<script>
function list_toggle(id) {
  const item = document.getElementById(id);
  if (!item) return;

  const isVisible = window.getComputedStyle(item).display !== "none";
  if (isVisible) {
    item.classList.remove("visible");
  } else {
    item.classList.add("visible");
  }
}
</script>

<div class="content" id="content">
  <div class="stories">
    <div id="actionbtns">
      
        <input type="hidden" name="id" value="{{ story.id }}"/>
        <button type="button" onclick="delete_entity('{{ story.id }}')" class="submitbtn">Delete</button>

    </div>
    <h1>{{ story.name }}</h1>

    <div class="description" id="description">
      {{ story.description }}
    </div>

    <div id="selectedItems" class="tagholder">
      {% for tag in tags %}
        <span class="tag">{{ tag.value }}</span>
      {% endfor %}
    </div>
    <div id="storyfragments" class="storyfragments entitylist">
      {% for fragment in fragments %}
        <div class="storyfragment">
          <button onclick="list_toggle('{{ fragment.id }}')" class="entitylink">
            {{ fragment.name }}
          </button>
          <div class="listitem" id="{{ fragment.id }}">
            <p>{{ fragment.content }}</p>
          </div>
        </div>
      {% endfor %}
    </div>

    <div class="actionitem">
      <a class="actionlink" href="/fragments/create?category=stories&id={{ story.id }}">
        Create Fragment
      </a>
      <a class="actionlink" href="/links/create?parent=stories&child=fragments&id={{ story.id }}">
        Add Existing Fragment
      </a>
    </div>

    <div id="storychars" class="storychars entitylist">
      {% for character in characters %}
        <div class="storychar entity">
          <a class="entitylink" href="/characters/{{ character.id }}">
            {{ character.name }}
          </a>
        </div>
      {% endfor %}
    </div>
    <div class="actionitem">
      <a class="actionlink" href="/characters/create?category=stories&id={{ story.id }}">
        Create Character
      </a>
      <a class="actionlink" href="/links/create?parent=stories&child=characters&id={{ story.id }}">
        Add Existing Character
      </a>
    </div>
    {{ tasks::taskform(parent=story.id, category="stories") }}
  </div>
</div>

</body>